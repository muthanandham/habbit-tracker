# Life-OS | Unified Daily Tracker

Welcome to **Life-OS**, a premium, AI-powered life optimization platform. Track your habits, manage tasks, monitor wellness, and reflect through journaling—all enhanced by an intelligent AI assistant.

---

## 🚀 Quick Start

### 1. Prerequisites
- **Node.js**: `v18.0.0` or higher
- **MongoDB** *(optional)*: A running local instance or a remote Atlas connection string. If unavailable, the app falls back to an **in-memory database** automatically.
- **AI Provider Key**: One of the following:
  - **Hugging Face** *(default)*: Get a token from [huggingface.co/settings/tokens](https://huggingface.co/settings/tokens)
  - **Gemini**: Get an API key from [Google AI Studio](https://aistudio.google.com/apikey)

### 2. Installation
```bash
git clone https://github.com/muthanandham/habbit-tracker.git
cd habbit-tracker
npm install
```

### 3. Environment Configuration
All environment configuration lives in a single file: **`server/.env`**

```bash
cp server/.env.example server/.env
```

Edit `server/.env` with your credentials:

```env
# Server
PORT=5000
NODE_ENV=development

# Database (falls back to in-memory if unavailable)
MONGODB_URI=mongodb://localhost:27017/life-os

# JWT Authentication
JWT_SECRET=your_secure_secret_key

# AI Provider — choose 'huggingface' or 'gemini'
AI_PROVIDER=huggingface

# Hugging Face (required when AI_PROVIDER=huggingface)
HF_TOKEN=your_huggingface_token

# Gemini (required when AI_PROVIDER=gemini)
# GEMINI_API_KEY=your_gemini_api_key
```

> **Note:** There is only one `.env` file (`server/.env`). Do not create a root-level `.env`.

### 4. Run the Application

#### ⚡ Start in Background (Recommended)
```bash
./start.sh
```
Starts the dev environment, creates a `logs/` directory, and tails the output automatically.

#### ⏹️ Stop Application
```bash
./stop.sh
```
Safely identifies and terminates the frontend and backend processes.

#### 🛠️ Direct Dev Mode
```bash
npm run dev
```

---

## 🛠️ Tech Stack
- **Frontend**: React (Vite), TypeScript, Tailwind CSS, Zustand, Lucide Icons
- **Backend**: Node.js, Express, MongoDB (Mongoose), AI-powered middleware
- **Intelligence**: Hugging Face Inference API / Gemini (configurable)
- **Testing**: Playwright (E2E), Vitest (Unit)
- **Styling**: Premium "Obsidian Archive" Dark Mode + Glassmorphism

---

## 📂 Project Structure
```text
├── client/          # Vite + React Frontend (TypeScript)
│   ├── src/         # UI components, pages, and logic
│   └── public/      # Static assets
├── server/          # Node.js + Express Backend
│   ├── src/         # Routes, controllers, and AI services
│   ├── .env         # ← Single environment config (not committed)
│   ├── .env.example # ← Template with all required variables
│   └── tests/       # Backend and Gateway test suites
├── shared/          # Shared TypeScript models/schemas
├── logs/            # Runtime logs generated by start.sh
├── docs/            # Full architectural & product documentation
└── package.json     # Root workspace configuration
```

---

## 🧠 AI Assistant Capabilities
The built-in **AI Life Assistant** can:
- Parse unstructured requests into habits/tasks.
- Provide "Pulse" insights on your wellness data.
- Analyze productivity patterns across your modules.
- *Actions are "safe" (Create/Update/Query only). Data deletion is restricted to maintain integrity.*

---

## 🧪 Testing & Quality
- **Full Verification**: `npm run test` (Runs all unit and E2E tests)
- **Backend Tests**: `npm run test --workspace=server`
- **E2E Browser Tests**: `npm run test:e2e` (Requires Playwright browsers)
- **Linting**: `npm run lint` (Checks all workspaces)

---

## 🛡️ Security & Hardening
Life-OS is built with production standards:
- **Rate Limiting**: AI endpoints are protected against abuse.
- **Input Sanitization**: Full validation using `express-validator`.
- **Security Headers**: Hardened with `helmet.js`.
- **Type Safety**: Strict TypeScript across the full stack.

---

## 📄 License
This project is for personal life optimization. All rights reserved.
